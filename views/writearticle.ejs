<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title%></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        html , body{
            height: 100vh;
            width: 100%;
        }
        .main{
            height: 100vh;
            width: 100%;
            background-color: #dee7e0;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 20px;
        
        }
        .sub-con{
            height: 90%;
            width: 40%;
            background-color: #000;
            display: flex;
            justify-content: center;
            flex-direction: column;
            align-items: center;
            border-radius: 20px;
            color: #dee7e0;
        }
        .form{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 80%;
        }
        input{
            background-color: #dee7e0;
        }

        a{
            color: #fff;
            /* text-decoration: none; */

        }
    </style>
</head>
<body>
    <div class="main">
        <div class="sub-con">
            <h3 class="fs-1">Write Article </h3>
            <form class="form" enctype="multipart/form-data" action="/write-article" method="post">
                <input type="text" name="Title" class="form-control mb-3 mt-3 title" id="" placeholder="Title">
                <input class="upload-Img form-control mb-3" name="image" type="file" />
                <div class="input-group">
                    <select class="form-select Category"  name="Category" id="inputGroupSelect04" aria-label="Example select with button addon">
                      <option selected>Choose...</option>
                      <option value="Business">Business</option>
                      <option value="Technology">Technology</option>
                      <option value="Politics">Politics</option>
                      <option value="Science">Science</option>
                      <option value="Health">Health</option>
                      <option value="Sports">Sports</option>
                      <option value="Entertainment">Entertainment</option>
                      <option value="Environment">Environment</option>
                      <option value="Lifestyl">Lifestyl</option>
                      <option value="Education">Education</option>
                    </select>
                    <!-- <button class="btn btn-outline-secondary" type="button">Button</button> -->
                  </div>
                <textarea class="form-control mt-4 mb-4 content" name="Content" id="" cols="30" rows="14" placeholder="Write full Deletes"></textarea>
                <button class="btn btn-secondary fs-4">Publich</button>
            </form>
        </div>
    </div>
    <script>
    document.querySelector('form').addEventListener('submit', function(event) {
    const titleInput = document.querySelector('.title');
    const contantInput = document.querySelector('.content');
    const CategoryInput = document.querySelector('.Category');

    
    // Check if the title input is empty
    if (!titleInput.value.trim()|| !contantInput.value.trim() || !CategoryInput.value.trim() ) {
      event.preventDefault(); // Prevent form submission
      
      // Show an alert to the user
      alert('Please fill all the details.');
    }
  });
    </script>
</body>
</html>